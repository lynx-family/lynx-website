name: Cherry-pick via Comment (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Comment-based cherry-pick is disabled."
          echo "Use the Cherry-pick request issue form and the cherry-pick:approved label instead."
