# OpenUI plan picker

This ReactLynx example streams an accumulated OpenUI Lang response into `OpenUiRenderer`, demonstrates local `$variables`, and forwards `@ToAssistant` actions to the host callback.

Run `pnpm --filter @lynx-example/openui dev` from the repository root to develop the example locally.
