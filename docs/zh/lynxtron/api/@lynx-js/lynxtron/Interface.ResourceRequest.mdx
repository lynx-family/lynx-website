[lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / ResourceRequest

# 接口：ResourceRequest

传给 `ProtocolHandler` 的请求对象。

## 属性

### resourceType

```ts
resourceType: string;
```

Lynx 资源类型，例如 `Generic`、`Image`、`Font`、`Lottie`、`Video`、`SVG`、
`Template`、`LynxCoreJS`、`LazyBundle` 或 `Unknown`。

#### 定义于

lynxtron/apis/api/protocol.d.ts

---

### scheme

```ts
scheme: string;
```

请求中已规范化的 URL scheme。

#### 定义于

lynxtron/apis/api/protocol.d.ts

---

### url

```ts
url: string;
```

正在处理的资源 URL。

#### 定义于

lynxtron/apis/api/protocol.d.ts
