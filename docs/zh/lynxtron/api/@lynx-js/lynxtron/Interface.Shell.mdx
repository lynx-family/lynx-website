import * as Lynx from '@lynx';

{/* This file is generated by @lynx-js/tool-typedoc. Do not edit this file directly. @generated */}
{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}

[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / Shell

# Shell

## 方法

### beep()

```ts
beep(): void
```

Play the beep sound.

**返回**

`void`

**定义于**

[apis/api/shell.d.ts:69](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L69)

---

### openExternal()

```ts
openExternal(url: string, options?: OpenExternalOptions): Promise<void>
```

Open the given external protocol URL in the desktop's default manner. (For
example, mailto: URLs in the user's default mail agent).

**参数**

| 范围       | 类型                                                                                       |
| ---------- | ------------------------------------------------------------------------------------------ |
| `url`      | `string`                                                                                   |
| `options`? | [`OpenExternalOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenExternalOptions.mdx) |

**返回**

`Promise`\<`void`\>

**定义于**

[apis/api/shell.d.ts:74](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L74)

---

### openPath()

```ts
openPath(path: string): Promise<string>
```

Resolves with a string containing the error message corresponding to the failure
if a failure occurred, otherwise "".

Open the given file in the desktop's default manner.

**参数**

| 范围   | 类型     |
| ------ | -------- |
| `path` | `string` |

**返回**

`Promise`\<`string`\>

**定义于**

[apis/api/shell.d.ts:81](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L81)

---

### readShortcutLink() <span className="rp-toc-exclude api-platform-badges-inline"><Lynx.PlatformBadge platform="windows" /></span>

```ts
readShortcutLink(shortcutPath: string): ShortcutDetails
```

Resolves the shortcut link at `shortcutPath`.

An exception will be thrown when any error happens.

**参数**

| 范围           | 类型     |
| -------------- | -------- |
| `shortcutPath` | `string` |

**返回**

[`ShortcutDetails`](/lynxtron/api/@lynx-js/lynxtron/Interface.ShortcutDetails.mdx)

**定义于**

[apis/api/shell.d.ts:89](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L89)

---

### showItemInFolder()

```ts
showItemInFolder(fullPath: string): void
```

Show the given file in a file manager. If possible, select the file.

**参数**

| 范围       | 类型     |
| ---------- | -------- |
| `fullPath` | `string` |

**返回**

`void`

**定义于**

[apis/api/shell.d.ts:93](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L93)

---

### trashItem()

```ts
trashItem(path: string): Promise<void>
```

Resolves when the operation has been completed. Rejects if there was an error
while deleting the requested item.

This moves a path to the OS-specific trash location (Trash on macOS, Recycle Bin
on Windows, and a desktop-environment-specific location on Linux).

**参数**

| 范围   | 类型     |
| ------ | -------- |
| `path` | `string` |

**返回**

`Promise`\<`void`\>

**定义于**

[apis/api/shell.d.ts:101](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L101)

---

### writeShortcutLink()

#### operation: "create" | "update" | "replace" <span className="rp-toc-exclude api-platform-badges-inline"><Lynx.PlatformBadge platform="windows" /></span>

```ts
writeShortcutLink(
   shortcutPath: string,
   operation: "create" | "update" | "replace",
   options: ShortcutDetails): boolean
```

Whether the shortcut was created successfully.

Creates or updates a shortcut link at `shortcutPath`.

**参数**

| 范围           | 类型                                                                               |
| -------------- | ---------------------------------------------------------------------------------- |
| `shortcutPath` | `string`                                                                           |
| `operation`    | `"create"` \| `"update"` \| `"replace"`                                            |
| `options`      | [`ShortcutDetails`](/lynxtron/api/@lynx-js/lynxtron/Interface.ShortcutDetails.mdx) |

**返回**

`boolean`

**定义于**

[apis/api/shell.d.ts:109](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L109)

#### options: ShortcutDetails <span className="rp-toc-exclude api-platform-badges-inline"><Lynx.PlatformBadge platform="windows" /></span>

```ts
writeShortcutLink(shortcutPath: string, options: ShortcutDetails): boolean
```

Whether the shortcut was created successfully.

Creates or updates a shortcut link at `shortcutPath`.

**参数**

| 范围           | 类型                                                                               |
| -------------- | ---------------------------------------------------------------------------------- |
| `shortcutPath` | `string`                                                                           |
| `options`      | [`ShortcutDetails`](/lynxtron/api/@lynx-js/lynxtron/Interface.ShortcutDetails.mdx) |

**返回**

`boolean`

**定义于**

[apis/api/shell.d.ts:121](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/shell.d.ts#L121)
