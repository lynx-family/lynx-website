[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / NativeImage

# 类: NativeImage

定义于: [lynxtron/apis/api/native-image.d.ts:103](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L103)

## 构造函数

### 构造函数

```ts
new NativeImage(): NativeImage;
```

#### 返回

`NativeImage`

## 属性

### isMacTemplateImage

```ts
isMacTemplateImage: boolean;
```

定义于: [lynxtron/apis/api/native-image.d.ts:267](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L267)

A `boolean` property that determines whether the image is considered a template
image.

Please note that this property only has an effect on macOS.

#### Platform

darwin

## 方法

### addRepresentation()

```ts
addRepresentation(options: AddRepresentationOptions): void;
```

定义于: [lynxtron/apis/api/native-image.d.ts:185](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L185)

Add an image representation for a specific scale factor. This can be used to
programmatically add different scale factor representations to an image. This
can be called on empty images.

#### 参数

| 参数      | 类型                                                                                                 |
| --------- | ---------------------------------------------------------------------------------------------------- |
| `options` | [`AddRepresentationOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.AddRepresentationOptions.mdx) |

#### 返回

`void`

---

### crop()

```ts
crop(rect: Rectangle): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:189](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L189)

The cropped image.

#### 参数

| 参数   | 类型                                                                   |
| ------ | ---------------------------------------------------------------------- |
| `rect` | [`Rectangle`](/lynxtron/api/@lynx-js/lynxtron/Interface.Rectangle.mdx) |

#### 返回

`NativeImage`

---

### getAspectRatio()

```ts
getAspectRatio(scaleFactor?: number): number;
```

定义于: [lynxtron/apis/api/native-image.d.ts:196](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L196)

The image's aspect ratio (width divided by height).

If `scaleFactor` is passed, this will return the aspect ratio corresponding to
the image representation most closely matching the passed value.

#### 参数

| 参数           | 类型     |
| -------------- | -------- |
| `scaleFactor?` | `number` |

#### 返回

`number`

---

### ~~getBitmap()~~

```ts
getBitmap(options?: BitmapOptions): void;
```

定义于: [lynxtron/apis/api/native-image.d.ts:202](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L202)

Legacy alias for `image.toBitmap()`.

#### 参数

| 参数       | 类型                                                                           |
| ---------- | ------------------------------------------------------------------------------ |
| `options?` | [`BitmapOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.BitmapOptions.mdx) |

#### 返回

`void`

#### 已被弃用

Use `image.toBitmap()` instead.

---

### getNativeHandle()

```ts
getNativeHandle(): Buffer;
```

定义于: [lynxtron/apis/api/native-image.d.ts:213](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L213)

A Buffer that stores C pointer to underlying native handle of the image. On
macOS, a pointer to `NSImage` instance is returned.

Notice that the returned pointer is a weak pointer to the underlying native
image instead of a copy, so you _must_ ensure that the associated `nativeImage`
instance is kept around.

#### 返回

`Buffer`

#### Platform

darwin

---

### getScaleFactors()

```ts
getScaleFactors(): number[];
```

定义于: [lynxtron/apis/api/native-image.d.ts:218](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L218)

An array of all scale factors corresponding to representations for a given
`NativeImage`.

#### 返回

`number`[]

---

### getSize()

```ts
getSize(scaleFactor?: number): Size;
```

定义于: [lynxtron/apis/api/native-image.d.ts:223](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L223)

If `scaleFactor` is passed, this will return the size corresponding to the image
representation most closely matching the passed value.

#### 参数

| 参数           | 类型     |
| -------------- | -------- |
| `scaleFactor?` | `number` |

#### 返回

[`Size`](/lynxtron/api/@lynx-js/lynxtron/Interface.Size.mdx)

---

### isEmpty()

```ts
isEmpty(): boolean;
```

定义于: [lynxtron/apis/api/native-image.d.ts:227](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L227)

Whether the image is empty.

#### 返回

`boolean`

---

### isTemplateImage()

```ts
isTemplateImage(): boolean;
```

定义于: [lynxtron/apis/api/native-image.d.ts:231](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L231)

Whether the image is a macOS template image.

#### 返回

`boolean`

---

### resize()

```ts
resize(options: ResizeOptions): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:238](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L238)

The resized image.

If only the `height` or the `width` are specified then the current aspect ratio
will be preserved in the resized image.

#### 参数

| 参数      | 类型                                                                           |
| --------- | ------------------------------------------------------------------------------ |
| `options` | [`ResizeOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.ResizeOptions.mdx) |

#### 返回

`NativeImage`

---

### setTemplateImage()

```ts
setTemplateImage(option: boolean): void;
```

定义于: [lynxtron/apis/api/native-image.d.ts:242](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L242)

Marks the image as a macOS template image.

#### 参数

| 参数     | 类型      |
| -------- | --------- |
| `option` | `boolean` |

#### 返回

`void`

---

### toBitmap()

```ts
toBitmap(options?: ToBitmapOptions): Buffer;
```

定义于: [lynxtron/apis/api/native-image.d.ts:246](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L246)

A Buffer that contains a copy of the image's raw bitmap pixel data.

#### 参数

| 参数       | 类型                                                                               |
| ---------- | ---------------------------------------------------------------------------------- |
| `options?` | [`ToBitmapOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.ToBitmapOptions.mdx) |

#### 返回

`Buffer`

---

### toDataURL()

```ts
toDataURL(options?: ToDataURLOptions): string;
```

定义于: [lynxtron/apis/api/native-image.d.ts:250](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L250)

The Data URL of the image.

#### 参数

| 参数       | 类型                                                                                 |
| ---------- | ------------------------------------------------------------------------------------ |
| `options?` | [`ToDataURLOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.ToDataURLOptions.mdx) |

#### 返回

`string`

---

### toJPEG()

```ts
toJPEG(quality: number): Buffer;
```

定义于: [lynxtron/apis/api/native-image.d.ts:254](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L254)

A Buffer that contains the image's `JPEG` encoded data.

#### 参数

| 参数      | 类型     |
| --------- | -------- |
| `quality` | `number` |

#### 返回

`Buffer`

---

### toPNG()

```ts
toPNG(options?: ToPNGOptions): Buffer;
```

定义于: [lynxtron/apis/api/native-image.d.ts:258](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L258)

A Buffer that contains the image's `PNG` encoded data.

#### 参数

| 参数       | 类型                                                                         |
| ---------- | ---------------------------------------------------------------------------- |
| `options?` | [`ToPNGOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.ToPNGOptions.mdx) |

#### 返回

`Buffer`

---

### createEmpty()

```ts
static createEmpty(): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:107](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L107)

Creates an empty `NativeImage` instance.

#### 返回

`NativeImage`

---

### createFromBitmap()

```ts
static createFromBitmap(buffer: Buffer, options: CreateFromBitmapOptions): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:112](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L112)

Creates a new `NativeImage` instance from `buffer` that contains the raw bitmap
pixel data returned by `toBitmap()`. The specific format is platform-dependent.

#### 参数

| 参数      | 类型                                                                                               |
| --------- | -------------------------------------------------------------------------------------------------- |
| `buffer`  | `Buffer`                                                                                           |
| `options` | [`CreateFromBitmapOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.CreateFromBitmapOptions.mdx) |

#### 返回

`NativeImage`

---

### createFromBuffer()

```ts
static createFromBuffer(buffer: Buffer, options?: CreateFromBufferOptions): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:120](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L120)

Creates a new `NativeImage` instance from `buffer`. Tries to decode as PNG or
JPEG first.

#### 参数

| 参数       | 类型                                                                                               |
| ---------- | -------------------------------------------------------------------------------------------------- |
| `buffer`   | `Buffer`                                                                                           |
| `options?` | [`CreateFromBufferOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.CreateFromBufferOptions.mdx) |

#### 返回

`NativeImage`

---

### createFromDataURL()

```ts
static createFromDataURL(dataURL: string): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:128](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L128)

Creates a new `NativeImage` instance from `dataUrl`, a base 64 encoded Data URL
string.

#### 参数

| 参数      | 类型     |
| --------- | -------- |
| `dataURL` | `string` |

#### 返回

`NativeImage`

---

### createFromNamedImage()

```ts
static createFromNamedImage(imageName: string, hslShift?: number[]): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:158](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L158)

Creates a new `NativeImage` instance from the `NSImage` that maps to the given
image name. See Apple's `NSImageName` documentation for a list of possible
values.

The `hslShift` is applied to the image with the following rules:

- `hsl_shift[0]` (hue): The absolute hue value for the image - 0 and 1 map to 0
  and 360 on the hue color wheel (red).
- `hsl_shift[1]` (saturation): A saturation shift for the image, with the
  following key values: 0 = remove all color. 0.5 = leave unchanged. 1 = fully
  saturate the image.
- `hsl_shift[2]` (lightness): A lightness shift for the image, with the
  following key values: 0 = remove all lightness (make all pixels black). 0.5 =
  leave unchanged. 1 = full lightness (make all pixels white).

This means that `[-1, 0, 1]` will make the image completely white and `[-1, 1,0]`
will make the image completely black.

In some cases, the `NSImageName` doesn't match its string representation; one
example of this is `NSFolderImageName`, whose string representation would
actually be `NSFolder`. Therefore, you'll need to determine the correct string
representation for your image before passing it in. This can be done with the
following:

where `SYSTEM_IMAGE_NAME` should be replaced with any value from this list.

#### 参数

| 参数        | 类型       |
| ----------- | ---------- |
| `imageName` | `string`   |
| `hslShift?` | `number`[] |

#### 返回

`NativeImage`

#### Platform

darwin

---

### createFromPath()

```ts
static createFromPath(path: string): NativeImage;
```

定义于: [lynxtron/apis/api/native-image.d.ts:167](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L167)

Creates a new `NativeImage` instance from an image file (e.g., PNG or JPEG)
located at `path`. This method returns an empty image if the `path` does not
exist, cannot be read, or is not a valid image.

#### 参数

| 参数   | 类型     |
| ------ | -------- |
| `path` | `string` |

#### 返回

`NativeImage`

---

### createThumbnailFromPath()

```ts
static createThumbnailFromPath(path: string, size: Size): Promise<NativeImage>;
```

定义于: [lynxtron/apis/api/native-image.d.ts:176](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/native-image.d.ts#L176)

fulfilled with the file's thumbnail preview image, which is a NativeImage.

Windows implementation will ignore `size.height` and scale the height
according to `size.width`.

#### 参数

| 参数   | 类型                                                         |
| ------ | ------------------------------------------------------------ |
| `path` | `string`                                                     |
| `size` | [`Size`](/lynxtron/api/@lynx-js/lynxtron/Interface.Size.mdx) |

#### 返回

`Promise`\<`NativeImage`\>

#### Platform

darwin,win32
