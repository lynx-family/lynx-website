[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / Settings

# 接口: Settings

定义于: [lynxtron/apis/api/app.d.ts:226](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L226)

## 属性

### args?

```ts
optional args?: string[];
```

定义于: [lynxtron/apis/api/app.d.ts:273](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L273)

The command-line arguments to pass to the executable. Defaults to an empty
array. Take care to wrap paths in quotes.

#### Platform

win32

---

### enabled?

```ts
optional enabled?: boolean;
```

定义于: [lynxtron/apis/api/app.d.ts:280](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L280)

`true` will change the startup approved registry key and `enable / disable` the
App in Task Manager and Windows Settings. Defaults to `true`.

#### Platform

win32

---

### name?

```ts
optional name?: string;
```

定义于: [lynxtron/apis/api/app.d.ts:286](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L286)

value name to write into registry. Defaults to the app's AppUserModelId().

#### Platform

win32

---

### ~~openAsHidden?~~

```ts
optional openAsHidden?: boolean;
```

定义于: [lynxtron/apis/api/app.d.ts:242](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L242)

`true` to open the app as hidden. Defaults to `false`. The user can edit this
setting from the System Preferences so
`app.getLoginItemSettings().wasOpenedAsHidden` should be checked when the app is
opened to know the current value. This setting is not available on MAS builds or
on macOS 13 and up.

#### 已被弃用

#### Platform

darwin

---

### openAtLogin?

```ts
optional openAtLogin?: boolean;
```

定义于: [lynxtron/apis/api/app.d.ts:231](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L231)

`true` to open the app at login, `false` to remove the app as a login item.
Defaults to `false`.

---

### path?

```ts
optional path?: string;
```

定义于: [lynxtron/apis/api/app.d.ts:266](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L266)

The executable to launch at login. Defaults to `process.execPath`.

#### Platform

win32

---

### serviceName?

```ts
optional serviceName?: string;
```

定义于: [lynxtron/apis/api/app.d.ts:260](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L260)

The name of the service. Required if `type` is non-default. Only available on
macOS 13 and up.

#### Platform

darwin

---

### type?

```ts
optional type?:
  | "mainAppService"
  | "agentService"
  | "daemonService"
  | "loginItemService";
```

定义于: [lynxtron/apis/api/app.d.ts:249](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/app.d.ts#L249)

The type of service to add as a login item. Defaults to `mainAppService`. Only
available on macOS 13 and up.

#### Platform

darwin
