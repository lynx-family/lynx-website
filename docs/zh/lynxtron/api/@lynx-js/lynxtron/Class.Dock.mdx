[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / Dock

# 类: Dock

定义于: [lynxtron/apis/api/dock.d.ts:8](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L8)

## 构造函数

### 构造函数

```ts
new Dock(): Dock;
```

#### 返回

`Dock`

## 方法

### bounce()

```ts
bounce(type?: "critical" | "informational"): number;
```

定义于: [lynxtron/apis/api/dock.d.ts:24](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L24)

an ID representing the request.

When `critical` is passed, the dock icon will bounce until either the
application becomes active or the request is canceled.

When `informational` is passed, the dock icon will bounce for one second.
However, the request remains active until either the application becomes active
or the request is canceled.

> [!NOTE] This method can only be used while the app is not focused; when the
> app is focused it will return -1.

#### 参数

| 参数    | 类型                              |
| ------- | --------------------------------- |
| `type?` | `"critical"` \| `"informational"` |

#### 返回

`number`

#### Platform

darwin

---

### cancelBounce()

```ts
cancelBounce(id: number): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:30](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L30)

Cancel the bounce of `id`.

#### 参数

| 参数 | 类型     |
| ---- | -------- |
| `id` | `number` |

#### 返回

`void`

#### Platform

darwin

---

### downloadFinished()

```ts
downloadFinished(filePath: string): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:36](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L36)

Bounces the Downloads stack if the filePath is inside the Downloads folder.

#### 参数

| 参数       | 类型     |
| ---------- | -------- |
| `filePath` | `string` |

#### 返回

`void`

#### Platform

darwin

---

### getBadge()

```ts
getBadge(): string;
```

定义于: [lynxtron/apis/api/dock.d.ts:42](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L42)

The badge string of the dock.

#### 返回

`string`

#### Platform

darwin

---

### getMenu()

```ts
getMenu(): Menu | null;
```

定义于: [lynxtron/apis/api/dock.d.ts:48](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L48)

The application's dock menu.

#### 返回

[`Menu`](/lynxtron/api/@lynx-js/lynxtron/Class.Menu.mdx) \| `null`

#### Platform

darwin

---

### hide()

```ts
hide(): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:54](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L54)

Hides the dock icon.

#### 返回

`void`

#### Platform

darwin

---

### isVisible()

```ts
isVisible(): boolean;
```

定义于: [lynxtron/apis/api/dock.d.ts:60](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L60)

Whether the dock icon is visible.

#### 返回

`boolean`

#### Platform

darwin

---

### setBadge()

```ts
setBadge(text: string): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:69](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L69)

Sets the string to be displayed in the dock’s badging area.

> [!IMPORTANT] You need to ensure that your application has the permission to
> display notifications for this method to work.

#### 参数

| 参数   | 类型     |
| ------ | -------- |
| `text` | `string` |

#### 返回

`void`

#### Platform

darwin

---

### setIcon()

```ts
setIcon(image:
  | string
  | NativeImage): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:75](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L75)

Sets the `image` associated with this dock icon.

#### 参数

| 参数    | 类型                                                                                  |
| ------- | ------------------------------------------------------------------------------------- |
| `image` | \| `string` \| [`NativeImage`](/lynxtron/api/@lynx-js/lynxtron/Class.NativeImage.mdx) |

#### 返回

`void`

#### Platform

darwin

---

### setMenu()

```ts
setMenu(menu: Menu): void;
```

定义于: [lynxtron/apis/api/dock.d.ts:81](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L81)

Sets the application's dock menu.

#### 参数

| 参数   | 类型                                                     |
| ------ | -------------------------------------------------------- |
| `menu` | [`Menu`](/lynxtron/api/@lynx-js/lynxtron/Class.Menu.mdx) |

#### 返回

`void`

#### Platform

darwin

---

### show()

```ts
show(): Promise<void>;
```

定义于: [lynxtron/apis/api/dock.d.ts:87](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dock.d.ts#L87)

Resolves when the dock icon is shown.

#### 返回

`Promise`\<`void`\>

#### Platform

darwin
