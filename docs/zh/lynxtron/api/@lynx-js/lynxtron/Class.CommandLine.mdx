import * as Lynx from '@lynx';

{/* This file is generated by @lynx-js/tool-typedoc. Do not edit this file directly. @generated */}
{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}

[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / CommandLine

# CommandLine

## 构造函数

### new CommandLine()

```ts
new CommandLine(): CommandLine
```

**返回**

[`CommandLine`](/lynxtron/api/@lynx-js/lynxtron/Class.CommandLine.mdx)

## 方法

### appendArgument()

```ts
appendArgument(value: string): void
```

Append an argument to the command line. The argument will be quoted correctly.
Switches will precede arguments regardless of appending order.

If you're appending an argument like `--switch=value`, consider using
`appendSwitch('switch', 'value')` instead.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control command-line behavior.

**参数**

| 范围    | 类型     |
| ------- | -------- |
| `value` | `string` |

**返回**

`void`

**定义于**

[apis/api/command-line.d.ts:16](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L16)

---

### appendSwitch()

```ts
appendSwitch(the_switch: string, value?: string): void
```

Append a switch (with optional `value`) to the command line.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control command-line behavior.

**参数**

| 范围         | 类型     |
| ------------ | -------- |
| `the_switch` | `string` |
| `value`?     | `string` |

**返回**

`void`

**定义于**

[apis/api/command-line.d.ts:23](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L23)

---

### getSwitchValue()

```ts
getSwitchValue(the_switch: string): string
```

The command-line switch value.

This function is meant to obtain command-line switches. It is not meant to be
used for application-specific command line arguments. For the latter, please use
`process.argv`.

> [!NOTE] When the switch is not present or has no value, it returns empty
> string.

**参数**

| 范围         | 类型     |
| ------------ | -------- |
| `the_switch` | `string` |

**返回**

`string`

**定义于**

[apis/api/command-line.d.ts:34](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L34)

---

### hasSwitch()

```ts
hasSwitch(the_switch: string): boolean
```

Whether the command-line switch is present.

**参数**

| 范围         | 类型     |
| ------------ | -------- |
| `the_switch` | `string` |

**返回**

`boolean`

**定义于**

[apis/api/command-line.d.ts:38](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L38)

---

### removeSwitch()

```ts
removeSwitch(the_switch: string): void
```

Removes the specified switch from the command line.

> [!NOTE] This will not affect `process.argv`. The intended usage of this
> function is to control command-line behavior.

**参数**

| 范围         | 类型     |
| ------------ | -------- |
| `the_switch` | `string` |

**返回**

`void`

**定义于**

[apis/api/command-line.d.ts:45](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L45)
