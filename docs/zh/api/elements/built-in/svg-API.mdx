import {
  APITable,
  AndroidOnly,
  ClayMacOSOnly,
  ClayOnly,
  ClayWindowsOnly,
  Deprecated,
  Go,
  HarmonyOnly,
  IOSOnly,
  PlatformTabs,
  Required,
  VersionBadge,
} from '@lynx';

## 属性

### `content`

<AndroidOnly /> <IOSOnly /> <ClayOnly /> <HarmonyOnly />

```tsx
content?: string;
```

SVG XML 内容

### `src`

<AndroidOnly /> <IOSOnly /> <ClayOnly /> <HarmonyOnly />

```tsx
src?: string;
```

SVG 资源 URL

## 事件

前端可以绑定相应的事件回调来监听元素的运行时行为，如下所示。

### `bindload`

<AndroidOnly /> <IOSOnly /> <ClayOnly /> <HarmonyOnly />

```tsx
bindload = (e: BaseEvent) => {};
```

SVG 已加载
