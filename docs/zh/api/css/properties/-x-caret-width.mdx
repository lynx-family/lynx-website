---
api: css/properties/-x-caret-width
---

import { APISummary, APITable } from '@lynx';

# -x-caret-width

<APISummary />

## 介绍

`-x-caret-width` 属性设置可编辑元件中文本插入光标的宽度。

## 语法

```css
input {
  -x-caret-width: 2px;
}
```

### 取值

- [`<length>`](/api/css/data-type/length.mdx)<br/>
  设置光标的宽度。

## 形式定义

import { PropertyDefinition } from '@/components/PropertyDefinition';

<PropertyDefinition
  initialValue={<>0px</>}
  appliesTo={
    <>
      <code>input</code> 和 <code>textarea</code>
    </>
  }
  inherited="no"
  animatable="no"
/>

## 形式语法

```
-x-caret-width = <length>
```

## 与 Web 的区别

- Lynx 特有的属性。

## 兼容性

<APITable />
