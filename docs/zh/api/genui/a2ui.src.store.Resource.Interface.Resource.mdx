import * as Lynx from '@lynx';

{/* This file is generated by @lynx-js/tool-typedoc. Do not edit this file directly. @generated */}
{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}

[genui](/api/genui/index.mdx) / [a2ui/src/store/Resource](/api/genui/a2ui.src.store.Resource.mdx) / Resource

# 接口: Resource\<T\>

Observable async resource used by the renderer to publish surface and
component updates.

## 类型参数

| 类型参数 | 默认类型  |
| -------- | --------- |
| `T`      | `unknown` |

## 属性

### id

```ts
id: string;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:23

---

### completed

```ts
readonly completed: boolean;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:24

---

### status

```ts
readonly status: ResourceStatus;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:25

---

### value

```ts
readonly value: undefined | T;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:26

---

### error

```ts
readonly error: unknown;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:27

---

### ~~read()~~

```ts
read: () => undefined | T;
```

#### 返回

`undefined` \| `T`

#### 已被弃用

Read `value` / `status` directly. Retained for backwards
compatibility; in the success state it returns the value, otherwise it
returns undefined. It does not throw.

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:33

---

### complete()

```ts
complete: (result: T) => void;
```

#### 参数

| 范围     | 类型 |
| -------- | ---- |
| `result` | `T`  |

#### 返回

`void`

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:34

---

### fail()

```ts
fail: (err: unknown) => void;
```

#### 参数

| 范围  | 类型      |
| ----- | --------- |
| `err` | `unknown` |

#### 返回

`void`

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:35

---

### ~~onUpdate()~~

```ts
onUpdate: (callback: (result: T) => void) => () => void;
```

#### 参数

| 范围       | 类型                      |
| ---------- | ------------------------- |
| `callback` | (`result`: `T`) => `void` |

#### 返回

`Function`

##### 返回

`void`

#### 已被弃用

Use `subscribe`. Behaves identically.

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:39

---

### subscribe()

```ts
subscribe: (callback: () => void) => () => void;
```

Subscribe to state changes. The callback is invoked after every
`complete()` or `fail()` (the same value may be re-published when the
processor re-emits an update for the same id). Returns a disposer.

#### 参数

| 范围       | 类型         |
| ---------- | ------------ |
| `callback` | () => `void` |

#### 返回

`Function`

##### 返回

`void`

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:45

---

### getSnapshot()

```ts
getSnapshot: () => ResourceSnapshot<T>;
```

Synchronous read of the current snapshot, suitable for
`useSyncExternalStore`. The returned object reference changes on every
transition.

#### 返回

[`ResourceSnapshot`](/api/genui/a2ui.src.store.Resource.Interface.ResourceSnapshot.mdx)\<`T`\>

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:51

---

### promise

```ts
promise: Promise<T>;
```

#### 定义于

@lynx-js/genui/a2ui/src/store/Resource.ts:52
