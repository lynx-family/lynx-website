---
title: performance
---

# performance

Options that trade build output for runtime performance: chunk splitting, console removal, runtime profiling and the bundle analyzer.

## At a glance

{/* @api ConfigOverview package="rspeedy" type="Config" path="performance" */}

| Name | Type | Description |
| --- | --- | --- |
| [`buildCache`](#performance-buildcache) <span className="api-badge api-badge-beta">Beta</span> | `boolean \| `[`BuildCache`](#performance-buildcache) | Enable or configure persistent build cache. |
| [`chunkSplit`](#performance-chunksplit) <span className="api-badge api-badge-deprecated">Deprecated</span> | `ChunkSplit \| ChunkSplitBySize \| ChunkSplitCustom` | `Performance.chunkSplit` is used to configure the chunk splitting strategy. |
| [`printFileSize`](#performance-printfilesize) | `boolean \| PrintFileSizeOptions` | Whether to print the file sizes after production build. |
| [`profile`](#performance-profile) | `boolean` | Whether capture timing information in Lynx runtime integrations such as ReactLynx. |
| [`removeConsole`](#performance-removeconsole) | `boolean \| ConsoleType[]` | Whether to remove `console.[methodName]` in production build. |

{/* @api-end */}

## Options

{/* @api ConfigOptions package="rspeedy" type="Config" path="performance" */}

### `performance.buildCache` <span className="api-badge api-badge-beta">Beta</span> \{#performance-buildcache\}

- **Type:** `boolean | `[`BuildCache`](#performance-buildcache)
- **Default:** ```ts
false

@beta This feature is experimental and may be changed in the future.
```

Enable or configure persistent build cache.

Enable persistent build cache.

```js
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    buildCache: true,
  },
})
```

Customize build cache.

```js
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    buildCache: {
      cacheDigest: [process.env.SOME_ENV],
      buildDependencies: ['postcss.config.js'],
    },
  },
})
```

#### `performance.buildCache.buildDependencies` <span className="api-badge api-badge-beta">Beta</span> \{#performance-buildcache-builddependencies\}

- **Type:** `string[]`
- **Default:** `['package.json', 'tsconfig.json' (or source.tsconfigPath), '.env', '.env.*', 'tailwindcss.config.*']`; when using the Rspeedy CLI with `performance.buildCache` enabled, the loaded `lynx.config.*` file is also added.

An array of files containing build dependencies.
Rspack will use the hash of each of these files to invalidate the persistent cache.

Add `postcss.config.js` to the build dependencies.

```js
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    buildCache: {
      buildDependencies: ['postcss.config.js'],
    },
  },
})
```

#### `performance.buildCache.cacheDigest` <span className="api-badge api-badge-beta">Beta</span> \{#performance-buildcache-cachedigest\}

- **Type:** `(string | undefined)[]`
- **Default:** `undefined`

Add additional cache digests, the previous build cache will be invalidated
when any value in the array changes.

Add `process.env.SOME_ENV` to the cache digest.

```js
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    buildCache: {
      cacheDigest: [process.env.SOME_ENV],
    },
  },
})
```

#### Other options \{#performance-buildcache-other-options\}

| Name | Type | Default | Description |
| --- | --- | --- | --- |
| <a id="performance-buildcache-cachedirectory"></a>`cacheDirectory` <span className="api-badge api-badge-beta">Beta</span> | `string` | `'node_modules/.cache'` | The output directory of the cache files. |

### `performance.chunkSplit` <span className="api-badge api-badge-deprecated">Deprecated</span> \{#performance-chunksplit\}

- **Type:** `ChunkSplit | ChunkSplitBySize | ChunkSplitCustom`
- **Default:** For web builds, Rsbuild currently uses `{ strategy: 'split-by-experience' }` when this option is unset.

 Use the top-level [`Config.splitChunks`](/rspeedy/config/splitChunks#splitchunks) option instead.

:::warning Deprecated
Deprecated.
:::

`Performance.chunkSplit` is used to configure the chunk splitting strategy.

### `performance.printFileSize` \{#performance-printfilesize\}

- **Type:** `boolean | PrintFileSizeOptions`
- **Default:** true

`Performance.printFileSize`

See [Rsbuild - performance.printFileSize](https://rsbuild.rs/config/performance/print-file-size) for details.

Whether to print the file sizes after production build.

If you don't want to print any information, you can disable it by setting printFileSize to false:

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: false
  },
})
```

Set total to false to disable total size output.

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: {
      total: false,
    },
  },
})
```

Set detail to false to disable per-asset size output.

If you don't need to view the size of each static asset, you can set detail to false. In this case, only the total size will be output:

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: {
      detail: false,
    },
  },
})
```

If you don't need to view the gzipped size, you can set compressed to false. This can save some gzip computation time for large projects:

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: {
      compressed: false,
    },
  },
})
```

To include only static assets that meet certain criteria, use a filter function with include.

If returned false, the static asset will be excluded and not included in the total size or detailed size.

only output static assets larger than 10kB:

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: {
      include: (asset) => asset.size > 10 * 1000,
    }
  },
})
```

To exclude static assets that meet certain criteria, use a filter function with exclude. If both include and exclude are set, exclude will take precedence.

Rspeedy defaults to excluding source map, license files, and .d.ts type files, as these files do not affect page load performance.

exclude .html files in addition to the default:

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    printFileSize: {
      exclude: (asset) =>
        /\.(?:map|LICENSE\.txt)$/.test(asset.name) ||
        /\.html$/.test(asset.name),
    }
  },
})
```

### `performance.profile` \{#performance-profile\}

- **Type:** `boolean`
- **Default:** Rspeedy sets this to `true` when `DEBUG` contains `rspeedy`; otherwise it leaves the option unset.

Whether capture timing information in Lynx runtime integrations such as ReactLynx.

Enable profile.

- Frameworks like ReactLynx will include runtime information using `console.profile`.
- Rspeedy will emit `dist/stats.json` after build for bundle analysis compatibility.

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: { profile: true },
})
```

### `performance.removeConsole` \{#performance-removeconsole\}

- **Type:** `boolean | ConsoleType[]`
- **Default:** `false`

Whether to remove `console.[methodName]` in production build.

- Remove all `console` methods

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    removeConsole: true,
  },
})
```

- Remove specific `console` methods

```ts
import { defineConfig } from '@lynx-js/rspeedy'

export default defineConfig({
  performance: {
    removeConsole: ['log', 'warn']
  },
})
```

{/* @api-end */}
