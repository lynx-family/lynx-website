---
title: Functions
---

import * as Lynx from '@lynx';

# Functions

Functions for crossing the thread boundary, creating elements outside JSX and mounting the root.

{/* @api ApiExports package="react" include="createPortal,createElement,cloneElement,runOnMainThread,runOnBackground,markFirstScreenSyncReady,withInitDataInState,root,Children" */}

## Functions \{#functions\}

### `createPortal()` \{#createportal\}

```ts
createPortal(vnode: ComponentChild, container: NodesRef): ReactNode
```

Renders children into a different ReactLynx element identified by a
`NodesRef` (e.g. from `ref={setX}` or `lynx.createSelectorQuery()`).

Context still flows across the portal boundary, and state updates inside the
portal behave like they do in any other component. A few behaviors differ
from `react-dom`:

- Events do not bubble along the React tree. ReactLynx follows Preact's
  approach, which has no synthetic event system, so events follow the actual
  element structure of the page.
- The portaled subtree renders on the background thread only, and does not
  participate in main-thread first-screen rendering.
- Mounting across pages or across native containers is not supported.

| Parameters | Type | Description |
| --- | --- | --- |
| `vnode` | `ComponentChild` | The React node to render into `container`. |
| `container` | `NodesRef` | The `NodesRef` target to render into. |

**Returns:** `ReactNode` — A `ReactNode` placeholder to include in the JSX at the call site; the rendered output goes into `container`.

{/* api-doc-overlay:react-create-portal-example:start */}

#### Example

<Lynx.Go
  example="react-apis"
  defaultFile="src/create-portal/index.tsx"
  defaultEntryFile="dist/create-portal.lynx.bundle"
  entry="src/create-portal"
  defaultTab="web"
/>

{/* api-doc-overlay:react-create-portal-example:end */}


### `createElement()` \{#createelement\}

```ts
createElement(type: Type, props?: IntrinsicElements[Type] | null, children: ReactNode[]): ReactElement<IntrinsicElements[Type], Type>
createElement(type: string | ComponentType<Props>, props?: Attributes & Props | null, children: ReactNode[]): ReactElement<Props>
```

Creates a ReactLynx element using the snapshot runtime.

| Parameters | Type | Description |
| --- | --- | --- |
| `type` | `Type` |  |
| `props?` | `IntrinsicElements[Type] \| null` |  |
| `children` | `ReactNode[]` |  |

**Returns:** `ReactElement<IntrinsicElements[Type], Type>`

{/* api-doc-overlay:react-create-element-example:start */}

#### Example

<Lynx.Go
  example="react-apis"
  defaultFile="src/create-element/index.tsx"
  defaultEntryFile="dist/create-element.lynx.bundle"
  entry="src/create-element"
  defaultTab="web"
/>

{/* api-doc-overlay:react-create-element-example:end */}


### `cloneElement()` \{#cloneelement\}

```ts
cloneElement(element: ReactElement<Props>, props?: Partial<Props> & Attributes | null, children: ReactNode[]): ReactElement<Props>
```

Clones a ReactLynx element using the snapshot runtime.

| Parameters | Type | Description |
| --- | --- | --- |
| `element` | `ReactElement<Props>` (from @types/react) |  |
| `props?` | `Partial<Props> & Attributes \| null` |  |
| `children` | `ReactNode[]` |  |

**Returns:** `ReactElement<Props>`

{/* api-doc-overlay:react-clone-element-example:start */}

#### Example

<Lynx.Go
  example="react-apis"
  defaultFile="src/clone-element/index.tsx"
  defaultEntryFile="dist/clone-element.lynx.bundle"
  entry="src/clone-element"
  defaultTab="web"
/>

{/* api-doc-overlay:react-clone-element-example:end */}


### `runOnMainThread()` \{#runonmainthread\}

```ts
runOnMainThread(fn: Fn): (args: Parameters<Fn>) => Promise<R>
```

`runOnMainThread` allows triggering main thread functions on the main thread asynchronously.

```ts
import { runOnMainThread } from '@lynx-js/react';

async function someFunction() {
  const fn = runOnMainThread(() => {
    'main thread';
    return 'hello';
  });
  const result = await fn();
}
```

| Parameters | Type | Description |
| --- | --- | --- |
| `fn` | `Fn` | The main thread functions to be called. |

**Returns:** `(args: Parameters<Fn>) => Promise<R>` — A function. Calling which with the arguments to be passed to the main thread function to trigger it on the main thread. This function returns a promise that resolves to the return value of the main thread function.

### `runOnBackground()` \{#runonbackground\}

```ts
runOnBackground(f: Fn): (args: Parameters<Fn>) => Promise<R>
```

`runOnBackground` allows triggering js functions on the background thread asynchronously.

| Parameters | Type | Description |
| --- | --- | --- |
| `f` | `Fn` | The js function to be called. |

**Returns:** `(args: Parameters<Fn>) => Promise<R>` — A function. Calling which with the arguments to be passed to the js function to trigger it on the background thread. This function returns a promise that resolves to the return value of the js function.

### `markFirstScreenSyncReady()` \{#markfirstscreensyncready\}

```ts
markFirstScreenSyncReady(): void
```

Mark the first screen as ready to sync when `firstScreenSyncTiming` is `'manual'`.

The main thread holds the UI control until this is called, so the handover timing to
the background thread (for hydration) is fully controlled by the user. It can be called
from both threads (a background-thread call is forwarded to the main thread), is a no-op
unless `firstScreenSyncTiming` is `'manual'`, and has no further effect once called.

```ts
import { markFirstScreenSyncReady } from "@lynx-js/react"

markFirstScreenSyncReady();
```

### `withInitDataInState()` \{#withinitdatainstate\}

```ts
withInitDataInState(App: ComponentClass<P, S>): ComponentClass<P, S>
```

Higher-Order Component (HOC) that injects `initData` into the state of the given class component.

This HOC checks if the provided component is a class component. If it is, it wraps the component
and injects the `initData` into its state. It also adds a listener
to update the state when data changes, and removes the listener when the component unmounts.

```typescript
class App extends React.Component<MyProps, MyState> {
  // component implementation
}

export default withInitDataInState(App);
```

| Parameters | Type | Description |
| --- | --- | --- |
| `App` | `ComponentClass<P, S>` (from @types/react) | The class component to be wrapped by the HOC. |

**Returns:** `ComponentClass<P, S>` — The original component if it is not a class component, otherwise a new class component          with `initData` injection and state update functionality.

## Constants \{#constants\}

### `root` \{#root\}

```ts
const root: Root
```

The default and only root of ReactLynx for you to render JSX

```ts
import { root } from "@lynx-js/react"
```

### `Children` \{#children\}

```ts
const Children: ReactLynxChildren
```

ReactLynx children utilities.

Arrays returned by `map`, `forEach`, and `toArray` are frozen.

{/* @api-end */}

## Built-in React APIs

`createContext`, `forwardRef`, `lazy`, `memo`, `createRef` and `isValidElement` are re-exported from React unchanged. See the [React reference](https://react.dev/reference/react/apis).
