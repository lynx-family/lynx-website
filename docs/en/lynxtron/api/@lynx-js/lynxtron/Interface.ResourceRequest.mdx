[lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / ResourceRequest

# Interface: ResourceRequest

Request object passed to a `ProtocolHandler`.

## Properties

### resourceType

```ts
resourceType: string;
```

The Lynx resource type, such as `Generic`, `Image`, `Font`, `Lottie`, `Video`,
`SVG`, `Template`, `LynxCoreJS`, `LazyBundle`, or `Unknown`.

#### Defined in

lynxtron/apis/api/protocol.d.ts

---

### scheme

```ts
scheme: string;
```

The normalized URL scheme for the request.

#### Defined in

lynxtron/apis/api/protocol.d.ts

---

### url

```ts
url: string;
```

The resource URL being handled.

#### Defined in

lynxtron/apis/api/protocol.d.ts
