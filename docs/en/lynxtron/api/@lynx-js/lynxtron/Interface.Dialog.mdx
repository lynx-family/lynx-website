import * as Lynx from '@lynx';

{/* This file is generated by @lynx-js/tool-typedoc. Do not edit this file directly. @generated */}
{/* Import all components as Lynx to allow dynamic lookup in TSDoc writings. */}

[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / Dialog

# Dialog

## Methods

### showErrorBox()

```ts
showErrorBox(title: string, content: string): void
```

Displays a modal dialog that shows an error message.

This API can be called safely before the `ready` event the `app` module emits,
it is usually used to report errors in early stage of startup. If called before
the app `ready`event on Linux, the message will be emitted to stderr, and no GUI
dialog will appear.

**Parameters**

| Parameter | Type     |
| --------- | -------- |
| `title`   | `string` |
| `content` | `string` |

**Returns**

`void`

**Defined in**

[apis/api/dialog.d.ts:396](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L396)

---

### showMessageBox()

#### window: BaseWindow

```ts
showMessageBox(window: BaseWindow, options: MessageBoxOptions): Promise<MessageBoxReturnValue>
```

resolves with a promise containing the following properties:

- `response` number - The index of the clicked button.
- `checkboxChecked` boolean - The checked state of the checkbox if
  `checkboxLabel` was set. Otherwise `false`.

Shows a message box.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                   |
| `options` | [`MessageBoxOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxOptions.mdx) |

**Returns**

`Promise`\<[`MessageBoxReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:409](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L409)

#### options: MessageBoxOptions

```ts
showMessageBox(options: MessageBoxOptions): Promise<MessageBoxReturnValue>
```

resolves with a promise containing the following properties:

- `response` number - The index of the clicked button.
- `checkboxChecked` boolean - The checked state of the checkbox if
  `checkboxLabel` was set. Otherwise `false`.

Shows a message box.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `options` | [`MessageBoxOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxOptions.mdx) |

**Returns**

`Promise`\<[`MessageBoxReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:425](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L425)

---

### showMessageBoxSync()

#### window: BaseWindow

```ts
showMessageBoxSync(window: BaseWindow, options: MessageBoxSyncOptions): number
```

the index of the clicked button.

Shows a message box, it will block the process until the message box is closed.
It returns the index of the clicked button.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal. If `window` is not shown dialog will not be attached to it. In
such case it will be displayed as an independent window.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                           |
| `options` | [`MessageBoxSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxSyncOptions.mdx) |

**Returns**

`number`

**Defined in**

[apis/api/dialog.d.ts:436](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L436)

#### options: MessageBoxSyncOptions

```ts
showMessageBoxSync(options: MessageBoxSyncOptions): number
```

the index of the clicked button.

Shows a message box, it will block the process until the message box is closed.
It returns the index of the clicked button.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal. If `window` is not shown dialog will not be attached to it. In
such case it will be displayed as an independent window.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `options` | [`MessageBoxSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.MessageBoxSyncOptions.mdx) |

**Returns**

`number`

**Defined in**

[apis/api/dialog.d.ts:450](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L450)

---

### showOpenDialog()

#### window: BaseWindow

```ts
showOpenDialog(window: BaseWindow, options: OpenDialogOptions): Promise<OpenDialogReturnValue>
```

Resolve with an object containing the following:

- `canceled` boolean - whether or not the dialog was canceled.
- `filePaths` string[] - An array of file paths chosen by the user. If the
  dialog is cancelled this will be an empty array.
- `bookmarks` string[] (optional) _macOS_ _mas_ - An array matching the
  `filePaths` array of base64 encoded strings which contains security scoped
  bookmark data. `securityScopedBookmarks` must be enabled for this to be
  populated. (For return values, see table here.)

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed or selected
when you want to limit the user to a specific type. For example:

The `extensions` array should contain extensions without wildcards or dots (e.g.
`'png'` is good but `'.png'` and `'*.png'` are bad). To show all files, use the
`'*'` wildcard (no other wildcard is supported).

> [!NOTE] On Windows and Linux an open dialog can not be both a file selector
> and a directory selector, so if you set `properties` to `['openFile',
'openDirectory']` on these platforms, a directory selector will be shown.

> [!NOTE] On Linux `defaultPath` is not supported when using portal file chooser
> dialogs unless the portal backend is version 4 or higher. You can use
> `--xdg-portal-required-version` command-line switch to force gtk or kde dialogs.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                   |
| `options` | [`OpenDialogOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogOptions.mdx) |

**Returns**

`Promise`\<[`OpenDialogReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:480](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L480)

#### options: OpenDialogOptions

```ts
showOpenDialog(options: OpenDialogOptions): Promise<OpenDialogReturnValue>
```

Resolve with an object containing the following:

- `canceled` boolean - whether or not the dialog was canceled.
- `filePaths` string[] - An array of file paths chosen by the user. If the
  dialog is cancelled this will be an empty array.
- `bookmarks` string[] (optional) _macOS_ _mas_ - An array matching the
  `filePaths` array of base64 encoded strings which contains security scoped
  bookmark data. `securityScopedBookmarks` must be enabled for this to be
  populated. (For return values, see table here.)

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed or selected
when you want to limit the user to a specific type. For example:

The `extensions` array should contain extensions without wildcards or dots (e.g.
`'png'` is good but `'.png'` and `'*.png'` are bad). To show all files, use the
`'*'` wildcard (no other wildcard is supported).

> [!NOTE] On Windows and Linux an open dialog can not be both a file selector
> and a directory selector, so if you set `properties` to `['openFile',
'openDirectory']` on these platforms, a directory selector will be shown.

> [!NOTE] On Linux `defaultPath` is not supported when using portal file chooser
> dialogs unless the portal backend is version 4 or higher. You can use
> `--xdg-portal-required-version` command-line switch to force gtk or kde dialogs.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `options` | [`OpenDialogOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogOptions.mdx) |

**Returns**

`Promise`\<[`OpenDialogReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:513](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L513)

---

### showOpenDialogSync()

#### window: BaseWindow

```ts
showOpenDialogSync(window: BaseWindow, options: OpenDialogSyncOptions): undefined | string[]
```

the file paths chosen by the user; if the dialog is cancelled it returns
`undefined`.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed or selected
when you want to limit the user to a specific type. For example:

The `extensions` array should contain extensions without wildcards or dots (e.g.
`'png'` is good but `'.png'` and `'*.png'` are bad). To show all files, use the
`'*'` wildcard (no other wildcard is supported).

> [!NOTE] On Windows and Linux an open dialog can not be both a file selector
> and a directory selector, so if you set `properties` to `['openFile',
'openDirectory']` on these platforms, a directory selector will be shown.

> [!NOTE] On Linux `defaultPath` is not supported when using portal file chooser
> dialogs unless the portal backend is version 4 or higher. You can use
> `--xdg-portal-required-version` command-line switch to force gtk or kde dialogs.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                           |
| `options` | [`OpenDialogSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogSyncOptions.mdx) |

**Returns**

`undefined` \| `string`[]

**Defined in**

[apis/api/dialog.d.ts:536](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L536)

#### options: OpenDialogSyncOptions

```ts
showOpenDialogSync(options: OpenDialogSyncOptions): undefined | string[]
```

the file paths chosen by the user; if the dialog is cancelled it returns
`undefined`.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed or selected
when you want to limit the user to a specific type. For example:

The `extensions` array should contain extensions without wildcards or dots (e.g.
`'png'` is good but `'.png'` and `'*.png'` are bad). To show all files, use the
`'*'` wildcard (no other wildcard is supported).

> [!NOTE] On Windows and Linux an open dialog can not be both a file selector
> and a directory selector, so if you set `properties` to `['openFile',
'openDirectory']` on these platforms, a directory selector will be shown.

> [!NOTE] On Linux `defaultPath` is not supported when using portal file chooser
> dialogs unless the portal backend is version 4 or higher. You can use
> `--xdg-portal-required-version` command-line switch to force gtk or kde dialogs.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `options` | [`OpenDialogSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.OpenDialogSyncOptions.mdx) |

**Returns**

`undefined` \| `string`[]

**Defined in**

[apis/api/dialog.d.ts:562](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L562)

---

### showSaveDialog()

#### window: BaseWindow

```ts
showSaveDialog(window: BaseWindow, options: SaveDialogOptions): Promise<SaveDialogReturnValue>
```

Resolve with an object containing the following:

- `canceled` boolean - whether or not the dialog was canceled.
- `filePath` string - If the dialog is canceled, this will be an empty string.
- `bookmark` string (optional) _macOS_ _mas_ - Base64 encoded string which
  contains the security scoped bookmark data for the saved file.
  `securityScopedBookmarks` must be enabled for this to be present. (For return
  values, see table here.)

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed, see
`dialog.showOpenDialog` for an example.

> [!NOTE] On macOS, using the asynchronous version is recommended to avoid
> issues when expanding and collapsing the dialog.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                   |
| `options` | [`SaveDialogOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogOptions.mdx) |

**Returns**

`Promise`\<[`SaveDialogReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:582](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L582)

#### options: SaveDialogOptions

```ts
showSaveDialog(options: SaveDialogOptions): Promise<SaveDialogReturnValue>
```

Resolve with an object containing the following:

- `canceled` boolean - whether or not the dialog was canceled.
- `filePath` string - If the dialog is canceled, this will be an empty string.
- `bookmark` string (optional) _macOS_ _mas_ - Base64 encoded string which
  contains the security scoped bookmark data for the saved file.
  `securityScopedBookmarks` must be enabled for this to be present. (For return
  values, see table here.)

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed, see
`dialog.showOpenDialog` for an example.

> [!NOTE] On macOS, using the asynchronous version is recommended to avoid
> issues when expanding and collapsing the dialog.

**Parameters**

| Parameter | Type                                                                                   |
| --------- | -------------------------------------------------------------------------------------- |
| `options` | [`SaveDialogOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogOptions.mdx) |

**Returns**

`Promise`\<[`SaveDialogReturnValue`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogReturnValue.mdx)\>

**Defined in**

[apis/api/dialog.d.ts:605](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L605)

---

### showSaveDialogSync()

#### window: BaseWindow

```ts
showSaveDialogSync(window: BaseWindow, options: SaveDialogSyncOptions): string
```

the path of the file chosen by the user; if the dialog is cancelled it returns
an empty string.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed, see
`dialog.showOpenDialog` for an example.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `window`  | [`BaseWindow`](/lynxtron/api/@lynx-js/lynxtron/Class.BaseWindow.mdx)                           |
| `options` | [`SaveDialogSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogSyncOptions.mdx) |

**Returns**

`string`

**Defined in**

[apis/api/dialog.d.ts:616](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L616)

#### options: SaveDialogSyncOptions

```ts
showSaveDialogSync(options: SaveDialogSyncOptions): string
```

the path of the file chosen by the user; if the dialog is cancelled it returns
an empty string.

The `window` argument allows the dialog to attach itself to a parent window,
making it modal.

The `filters` specifies an array of file types that can be displayed, see
`dialog.showOpenDialog` for an example.

**Parameters**

| Parameter | Type                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| `options` | [`SaveDialogSyncOptions`](/lynxtron/api/@lynx-js/lynxtron/Interface.SaveDialogSyncOptions.mdx) |

**Returns**

`string`

**Defined in**

[apis/api/dialog.d.ts:630](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L630)
