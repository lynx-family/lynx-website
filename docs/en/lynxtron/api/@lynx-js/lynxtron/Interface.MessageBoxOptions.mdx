[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / MessageBoxOptions

# Interface: MessageBoxOptions

Defined in: [lynxtron/apis/api/dialog.d.ts:13](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L13)

## Properties

### buttons?

```ts
optional buttons?: string[];
```

Defined in: [lynxtron/apis/api/dialog.d.ts:28](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L28)

Array of texts for buttons. On Windows, an empty array will result in one button
labeled "OK".

---

### cancelId?

```ts
optional cancelId?: number;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:70](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L70)

The index of the button to be used to cancel the dialog, via the `Esc` key. By
default this is assigned to the first button with "cancel" or "no" as the label.
If no such labeled buttons exist and this option is not set, `0` will be used as
the return value.

---

### checkboxChecked?

```ts
optional checkboxChecked?: boolean;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:56](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L56)

Initial checked state of the checkbox. `false` by default.

---

### checkboxLabel?

```ts
optional checkboxLabel?: string;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:52](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L52)

If provided, the message box will include a checkbox with the given label.

---

### defaultId?

```ts
optional defaultId?: number;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:33](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L33)

Index of the button in the buttons array which will be selected by default when
the message box opens.

---

### detail?

```ts
optional detail?: string;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:48](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L48)

Extra information of the message.

---

### icon?

```ts
optional icon?:
  | string
  | NativeImage;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:57](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L57)

---

### message

```ts
message: string;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:17](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L17)

Content of the message box.

---

### noLink?

```ts
optional noLink?: boolean;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:77](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L77)

On Windows, Lynxtron will try to figure out which of the `buttons` are common
buttons (like "Cancel" or "Yes"), and show the others as command links in the
dialog. This can make the dialog appear in the style of modern Windows apps. If
you don't like this behavior, you can set `noLink` to `true`.

---

### normalizeAccessKeys?

```ts
optional normalizeAccessKeys?: boolean;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:87](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L87)

Normalize the keyboard access keys across platforms. Default is `false`.
Enabling this assumes `&` is used in the button labels for the placement of the
keyboard shortcut access key and labels will be converted so they work correctly
on each platform, `&` characters are removed on macOS, converted to `_` on
Linux, and left untouched on Windows. For example, a button label of `Vie&w`
will be converted to `Vie_w` on Linux and `View` on macOS and can be selected
via `Alt-W` on Windows and Linux.

---

### signal?

```ts
optional signal?: AbortSignal;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:40](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L40)

Pass an instance of AbortSignal to optionally close the message box, the message
box will behave as if it was cancelled by the user. On macOS, `signal` does not
work with message boxes that do not have a parent window, since those message
boxes run synchronously due to platform limitations.

---

### textWidth?

```ts
optional textWidth?: number;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:63](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L63)

Custom width of the text in the message box.

#### Platform

darwin

---

### title?

```ts
optional title?: string;
```

Defined in: [lynxtron/apis/api/dialog.d.ts:44](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L44)

Title of the message box, some platforms will not show it.

---

### type?

```ts
optional type?: "error" | "none" | "info" | "warning" | "question";
```

Defined in: [lynxtron/apis/api/dialog.d.ts:23](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/dialog.d.ts#L23)

Can be `none`, `info`, `error`, `question` or `warning`. On Windows, `question`
displays the same icon as `info`, unless you set an icon using the `icon`
option. On macOS, both `warning` and `error` display the same warning icon.
