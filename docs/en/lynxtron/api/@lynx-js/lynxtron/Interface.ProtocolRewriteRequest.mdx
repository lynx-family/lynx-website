[lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / ProtocolRewriteRequest

# Interface: ProtocolRewriteRequest

Request object passed to a `ProtocolRequestRewriter`.

Request rewriting runs before protocol handlers and fallback loaders.

## Properties

### scheme

```ts
scheme: string;
```

The normalized URL scheme for the request.

#### Defined in

lynxtron/apis/api/protocol.d.ts

---

### url

```ts
url: string;
```

The original resource URL.

#### Defined in

lynxtron/apis/api/protocol.d.ts
