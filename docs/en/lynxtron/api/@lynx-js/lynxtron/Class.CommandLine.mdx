[@lynx-js/lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / CommandLine

# Class: CommandLine

Defined in: [lynxtron/apis/api/command-line.d.ts:5](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L5)

## Constructors

### Constructor

```ts
new CommandLine(): CommandLine;
```

#### Returns

`CommandLine`

## Methods

### appendArgument()

```ts
appendArgument(value: string): void;
```

Defined in: [lynxtron/apis/api/command-line.d.ts:16](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L16)

Append an argument to the command line. The argument will be quoted correctly.
Switches will precede arguments regardless of appending order.

If you're appending an argument like `--switch=value`, consider using
`appendSwitch('switch', 'value')` instead.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control command-line behavior.

#### Parameters

| Parameter | Type     |
| --------- | -------- |
| `value`   | `string` |

#### Returns

`void`

---

### appendSwitch()

```ts
appendSwitch(the_switch: string, value?: string): void;
```

Defined in: [lynxtron/apis/api/command-line.d.ts:23](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L23)

Append a switch (with optional `value`) to the command line.

[!NOTE] This will not affect `process.argv`. The intended usage of this
function is to control command-line behavior.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |
| `value?`     | `string` |

#### Returns

`void`

---

### getSwitchValue()

```ts
getSwitchValue(the_switch: string): string;
```

Defined in: [lynxtron/apis/api/command-line.d.ts:34](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L34)

The command-line switch value.

This function is meant to obtain command-line switches. It is not meant to be
used for application-specific command line arguments. For the latter, please use
`process.argv`.

> [!NOTE] When the switch is not present or has no value, it returns empty
> string.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`string`

---

### hasSwitch()

```ts
hasSwitch(the_switch: string): boolean;
```

Defined in: [lynxtron/apis/api/command-line.d.ts:38](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L38)

Whether the command-line switch is present.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`boolean`

---

### removeSwitch()

```ts
removeSwitch(the_switch: string): void;
```

Defined in: [lynxtron/apis/api/command-line.d.ts:45](https://github.com/lynx-family/lynxtron/blob/main/src/packages/lynxtron/apis/api/command-line.d.ts#L45)

Removes the specified switch from the command line.

> [!NOTE] This will not affect `process.argv`. The intended usage of this
> function is to control command-line behavior.

#### Parameters

| Parameter    | Type     |
| ------------ | -------- |
| `the_switch` | `string` |

#### Returns

`void`
