[lynxtron](/lynxtron/api/@lynx-js/lynxtron/index.mdx) / ResourceResponse

# Interface: ResourceResponse

Response object returned by a `ProtocolHandler`.

At runtime, a response is accepted only when it includes `statusCode` and
`Buffer` data. Invalid responses fall back to the normal Lynx resource loading
path.

## Properties

### url

```ts
url: string;
```

The response URL.

#### Defined in

lynxtron/apis/api/protocol.d.ts

---

### statusCode

```ts
statusCode: number;
```

The HTTP-style status code for the response.

#### Defined in

lynxtron/apis/api/protocol.d.ts

---

### data

```ts
data: Buffer;
```

The response body as a Node.js `Buffer`.

#### Defined in

lynxtron/apis/api/protocol.d.ts
