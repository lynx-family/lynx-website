import { APIBadge, APITable, AndroidOnly, IOSOnly, HarmonyOnly, VersionBadge } from '@lynx';
import blurViewData from '@lynx-js/lynx-compat-data/elements/blur-view.json';

## Attributes

### `android-capture-target`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/android-capture-target" />

```tsx
'android-capture-target'?: string;
```

The raw id of the Android Lynx view to capture and blur.
Only the overlap with the 
`<blur-view>`
 is used as the blur source.
When omitted or unresolved, Android uses the default parent capture path.


### `blur-effect`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/blur-effect" />

```tsx
// @defaultValue: 'light'
'blur-effect'?: 'light' | 'extra-light' | 'dark' | 'glass' | 'glass-container';
```

It mainly affects the brightness of the blurred area:
light: the brightness is basically the same as the background;
dark: the brightness is darker than the background;
extra-light: the brightness is brighter than the background;
glass: a visual effect that renders a glass material;
glass-container: a UIGlassContainerEffect renders multiple glass elements into a combined effect.


### `blur-radius`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/blur-radius" />

```tsx
// @defaultValue: "0px"
'blur-radius'?: string;
```

Gaussian blur radius.


### `blur-sampling`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/blur-sampling" />

```tsx
// @defaultValue: 6
'blur-sampling'?: number;
```

The downsampling ratio of the blurred area is similar to reducing the blur of the background area first and then zooming in,
which slightly affects the blur effect, but can significantly improve performance.


### `enable-auto-blur`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/enable-auto-blur" />

```tsx
// @defaultValue: true
'enable-auto-blur'?: boolean;
```

Whether to automatically update blur.


### `experimental-update-blur-radius`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/experimental-update-blur-radius" />

```tsx
'experimental-update-blur-radius'?: boolean;
```

Switches the Android internal blur-buffer refresh path.


### `glass-interactive`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/glass-interactive" />

```tsx
// @defaultValue: false
'glass-interactive'?: boolean;
```

Enables interactive behavior for the glass effect.


### `glass-style`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/glass-style" />

```tsx
// @defaultValue: 'regular'
'glass-style'?: 'clear' | 'regular';
```

The style of the glass effect.


### `glass-tint-color`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/glass-tint-color" />

```tsx
// @defaultValue: 'transparent'
'glass-tint-color'?: string;
```

A tint color applied to the glass effect.


### `spacing`

<APIBadge data={blurViewData.elements['blur-view']} query="attributes/spacing" />

```tsx
// @defaultValue: 0
spacing?: number;
```

The spacing specifies the distance between elements at which they begin to merge.

