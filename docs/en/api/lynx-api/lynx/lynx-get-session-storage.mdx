---
title: getSessionStorageItem
api: lynx-api/lynx/getSessionStorageItem
---

import { APISummary, APITable } from '@lynx';

# lynx: getSessionStorageItem() static method

<APISummary />

The `lynx.getSessionStorageItem` method is used to retrieve data shared across multiple LynxViews. This enables data sharing between multiple pages via `SessionStorage`. You can call [lynx.setSessionStorageItem](./lynx-set-session-storage.mdx) to set the cached data.

## Syntax

```ts
lynx.getSessionStorageItem(key, (value) => {});
```

### Parameters

#### key

The key for the cached data, used to retrieve data and listen for changes.

#### callback

The callback function for retrieving cached data. The argument is the cached data object.

### Return Value

None (`undefined`).

## Compatibility

<APITable />
