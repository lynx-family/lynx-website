# `<Input>`

输入框组件，支持受控与非受控模式，并内置键盘避让能力。使用 `<Input>` 进行单行输入，使用 `<TextArea>` 进行多行输入。

import Introduction from '../../introDocs/lynx-ui-input/Introduction.zh.mdx';
import APIReference from './APIReference.mdx';

<Introduction />
<APIReference />
