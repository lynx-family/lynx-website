## 进阶用法

`<Swiper>`支持布局和动画的完全自定义，其中的关键是 `customAnimation` 和 `mode`

import { Columns } from '@lynx/index';
import CustomAnimationValue0Pic from '@assets/Swiper/custom_animation_value_0.png';
import ModeNormalPic from '@assets/Swiper/mode_normal.png';
import ModeCustomPic from '@assets/Swiper/mode_custom.png';
import DemoCustomPic from '@assets/Swiper/Custom.webp';

### 理解 `customAnimation`

```
type customAnimation = (value: number, index: number) => CSSProperties
```

`customAnimation`是一个主线程函数。在`<Swiper>`滚动时，每一个`<SwiperItem>`都会运行`customAnimation`函数，并根据其返回值更新自己的样式。

其中，函数入参`value`表示这个`<SwiperItem>`相对于选中`<SwiperItem>`的距离。

<Columns>
<img
src={CustomAnimationValue0Pic}
width="60%"
/>

<div>
在这个例子中，`<Swiper>`的进度为 2，于是：
- `<SwiperItem>-2`收到的`value`是 0
- `<SwiperItem>-1`收到的`value`是 -1
- `<SwiperItem>-3`收到的`value`是 1
</div>

</Columns>

<Columns>
<img
src={CustomAnimationValue0Pic}
width="60%"
/>

<div>
更进一步，`<Swiper>`的进度被更新到 3 时，会有：
- `<SwiperItem>-2`收到的`value`是 -1
- `<SwiperItem>-1`收到的`value`是 -2
- `<SwiperItem>-3`收到的`value`是 0
</div>

</Columns>

拿到`<SwiperItem>`的位置之后，可以根据位置决定这个`<SwiperItem>`的位置与样式。

### `customAnimation`的实践

import { Go } from '@lynx/index';

<Go
  img={DemoCustomPic}
  example="Swiper"
  defaultFile="Custom/index.tsx"
  defaultEntryName="SwiperCustom"
  entry="Custom"
/>

## 理解 `mode`

`<Swiper>`支持`normal`和`custom`两种`mode`。他们的区别如下：

<Columns>
<img
src={ModeNormalPic}
width="60%"
/>

<div>
`mode='normal'`

- `<SwiperItem>`按照方向依次排列
- 根据 `modeConfig` 提供了居中，向右对齐等能力

</div>

</Columns>

<Columns>
<img
src={ModeCustomPic}
width="60%"
/>

<div>
`mode='custom'`

- `<SwiperItem>` 的位置需要通过 [`customAnimation`](#理解-customanimation)指定
- 自定义的能力更高

</div>

</Columns>

总结起来

- `normal` mode 处理了`<SwiperItem>`的位置，比较适合横滑的场景，使用起来更方便
- `custom` mode 需要对位置进行手动计算，但是扩展性更高

### `mode='custom'` 时 `customAnimation` 的实践

<Go
  img={DemoCustomPic}
  example="Swiper"
  defaultFile="CustomScale/index.tsx"
  defaultEntryName="SwiperCustomScale"
  entry="CustomScale"
/>
