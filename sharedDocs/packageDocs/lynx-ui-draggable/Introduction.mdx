import { Go, GoRightFooter } from '@lynx-ui/index';

## Basic Usage

<Go
  img=""
  example="Draggable"
  defaultEntryName="DraggableBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
  rightFooter={<GoRightFooter demoName="draggable-basic" version="0.0.6" />}
/>

## With bounds

The draggable area allowed can be limited with min(max)TranslateX/Y props.

<Go
  img=""
  example="Draggable"
  defaultEntryName="DraggableWithBounds"
  defaultFile="WithBounds/index.tsx"
  entry="WithBounds"
  rightFooter={
    <GoRightFooter demoName="draggable-with-bounds" version="0.0.6" />
  }
/>

## Structure

The whole component is draggable.

```tsx
<Draggable />
```

Only the DraggableArea accepts the dragging gesture.

```tsx
<DraggableRoot>
  <DraggableArea />
</DraggableRoot>
```

## API
