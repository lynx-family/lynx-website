import { Go, ClassRenderPropTable } from '@lynx-ui/index';

单选组是一组可勾选的按钮（即单选按钮），一次只能选择其中一个。

## 基础用法

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/lynx-ui-cover-radio-group.webm"
  example="lynx-ui-radio-group"
  defaultEntryName="RadioGroupBasic"
  entry="Basic"
/>

## 结构

```jsx
<RadioGroupRoot>
  <Radio>
    <RadioIndicator />
  </Radio>
</RadioGroupRoot>
```

## 样式

本部分展示 `<Radio>` 和 `<RadioIndicator>` 中的内部状态，以及 `<RadioGroupRoot>` 内提供的 `Render Prop`（仅限 `disabled`）。

<ClassRenderPropTable
  source={[
    {
      className: 'ui-disabled',
      'Render Prop': 'disabled',
      item: { type: 'boolean', description: 'Radio 是否禁用。' },
    },
    {
      className: 'ui-active',
      item: {
        type: 'boolean',
        description: 'Radio 是否处于激活态（按下或交互中）。',
      },
    },
    {
      className: 'ui-checked',
      item: { type: 'boolean', description: 'Radio 是否选中。' },
    },
  ]}
/>

## API
