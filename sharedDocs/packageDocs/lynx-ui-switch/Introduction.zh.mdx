import { Go, ClassRenderPropTable } from '@lynx-ui/index';

## 基础用法

<Go
  img=""
  example="Switch"
  defaultEntryName="Basic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
/>

## 结构

```tsx
<Switch>
  <SwitchThumb />
</Switch>
```

```tsx
<Switch>
  <SwitchTrack />
  <SwitchThumb />
</Switch>
```

## 样式

本部分展示 `<Switch>`、`<SwitchThumb>` 和 `<SwitchTrack>` 的内部状态。

<ClassRenderPropTable
  source={[
    {
      className: 'ui-disabled',
      'Render Prop': 'disabled',
      item: { type: 'boolean', description: 'Switch 是否为禁用状态' },
    },
    {
      className: 'ui-active',
      'Render Prop': 'active',
      item: {
        type: 'boolean',
        description: 'Switch 是否处于激活态（按下或交互中）。',
      },
    },
    {
      className: 'ui-checked',
      'Render Prop': 'checked',
      item: { type: 'boolean', description: 'Switch 是否为选中状态' },
    },
  ]}
/>

## API
