import { Go, ClassRenderPropTable } from '@lynx-ui/index';

## Basic Usage

<Go
  img="https://lf-lynx.tiktok-cdns.com/obj/lynx-artifacts-oss-sg/lynx-website/assets/lynx-ui/lynx-ui-cover-switch.webm"
  example="lynx-ui-switch"
  defaultEntryName="SwitchBasic"
  entry="Basic"
/>

## Structure

```tsx
<Switch>
  <SwitchThumb />
</Switch>
```

```tsx
<Switch>
  <SwitchTrack />
  <SwitchThumb />
</Switch>
```

## Styling

This section shows the internal state of `<Switch>`, `<SwitchThumb>`, and `<SwitchTrack>`.

<ClassRenderPropTable
  source={[
    {
      className: 'ui-disabled',
      'Render Prop': 'disabled',
      item: { type: 'boolean', description: 'Whether the Switch is disabled' },
    },
    {
      className: 'ui-active',
      'Render Prop': 'active',
      item: {
        type: 'boolean',
        description: 'Whether the Switch is in an active or pressed state.',
      },
    },
    {
      className: 'ui-checked',
      'Render Prop': 'checked',
      item: { type: 'boolean', description: 'Whether the Switch is checked' },
    },
  ]}
/>

## API
