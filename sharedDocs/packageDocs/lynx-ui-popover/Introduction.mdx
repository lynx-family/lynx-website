import { Go, ClassRenderPropTable } from '@lynx/index';

The popover component allows you to set different positioning strategies and styles, supports custom content, and supports custom arrows.

## Basic Usage

<Go
  img=""
  example="PrimitivesPopover"
  defaultEntryName="PrimitivesPopoverBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
/>

## Extra Anchor

Use 'crossAxisOffset' on PopoverPositioner to set the skidding offset of the floating element.

<Go
  img=""
  example="PrimitivesPopover"
  defaultEntryName="PrimitivesPopoverExtraAnchor"
  defaultFile="ExtraAnchor/index.tsx"
  entry="ExtraAnchor"
/>

## Cross axis offset adjustment

If you want to add an extra anchor, you can use the PopoverAnchor Component.

<Go
  img=""
  example="PrimitivesPopover"
  defaultEntryName="PrimitivesPopoverCrossAxisAdjustment"
  defaultFile="CrossAxisAdjustment/index.tsx"
  entry="CrossAxisAdjustment"
/>

## Custom Arrow

If you want to customize the arrow, you can pass a custom children to the `PopoverArrow` to customize its appearance.

<Go
  img=""
  example="PrimitivesPopover"
  defaultEntryName="PrimitivesPopoverCustomArrow"
  defaultFile="CustomArrow/index.tsx"
  entry="CustomArrow"
/>

## Structure

```jsx
<PopoverRoot>
  <PopoverTrigger />
  <PopoverAnchor>
    <PopoverPositioner>
      <PopoverContent />
      <PopoverArrow />
    </PopoverPositioner>
  </PopoverAnchor>
</PopoverRoot>
```

## Styling

This section shows animation-related lifecycle in components. Class names appear when the `transition` prop is true, and render props are always available.

<ClassRenderPropTable
  source={[
    {
      className: 'ui-open',
      'Render Prop': 'open',
      item: { type: 'boolean', description: 'The popper is opened.' },
    },
    {
      className: 'ui-entering',
      'Render Prop': 'entering',
      item: {
        type: 'boolean',
        description:
          'The popper is doing the entering animation. Takes effect when transition:true.',
      },
    },
    {
      className: 'ui-leaving',
      'Render Prop': 'leaving',
      item: {
        type: 'boolean',
        description:
          'The popper is doing the leaving animation. Takes effect when transition:true.',
      },
    },
    {
      className: 'ui-animating',
      'Render Prop': 'animating',
      item: {
        type: 'boolean',
        description:
          'The popper is doing the animations. Takes effect when transition:true.',
      },
    },
  ]}
/>

## API
