import { Go, ClassRenderPropTable } from '@lynx-ui/index';

弹窗组件，可以设置背景遮罩层。

## 基础用法

<Go
  img=""
  example="lynx-ui-dialog"
  defaultEntryName="DialogBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
/>

## 结构

```jsx
<DialogRoot>
  <DialogView>
    <DialogBackdrop />
    <DialogContent />
  </DialogView>
</DialogRoot>
```

## 样式

本节展示组件中与动画相关的生命周期。当 `transition` 属性为 `true` 时，额外的 className 会被添加到组件的 className 上，而 render props 始终可用。

<ClassRenderPropTable
  source={[
    {
      className: 'ui-open',
      'Render Prop': 'open',
      item: { type: 'boolean', description: '弹窗是否打开。' },
    },
    {
      className: 'ui-closed',
      'Render Prop': 'closed',
      item: {
        type: 'boolean',
        description:
          '弹窗是否关闭。如果是强制挂载的弹窗，初始状态会是 ui-closed。',
      },
    },
    {
      className: 'ui-entering',
      'Render Prop': 'entering',
      item: {
        type: 'boolean',
        description: '弹出框正在执行进入动画。transition:true 时生效。',
      },
    },
    {
      className: 'ui-leaving',
      'Render Prop': 'leaving',
      item: {
        type: 'boolean',
        description: '弹出框正在执行退出动画。transition:true 时生效。',
      },
    },
    {
      className: 'ui-animating',
      'Render Prop': 'animating',
      item: {
        type: 'boolean',
        description: '弹出框正在执行进入/退出动画。transition:true 时生效。',
      },
    },
  ]}
/>

## API
