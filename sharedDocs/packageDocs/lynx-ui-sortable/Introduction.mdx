import { Go, GoRightFooter } from '@lynx-ui/index';

A sorting component implemented based on the Draggable component. Currently, it only supports child items of equal height, and there should be no gaps (such as margins) between child items.

## Basic Usage

<Go
  img=""
  example="Sortable"
  defaultEntryName="SortableBasic"
  defaultFile="Basic/index.tsx"
  entry="Basic"
  rightFooter={<GoRightFooter demoName="sortable-basic" version="0.0.5" />}
/>

## Drag Without a boundary

If no boundaryId is provided, the SortableItem can be dragged freely without restrictions.

<Go
  img=""
  example="Sortable"
  defaultEntryName="SortableNoBoundary"
  defaultFile="NoBoundary/index.tsx"
  entry="NoBoundary"
  rightFooter={
    <GoRightFooter demoName="sortable-no-boundary" version="0.0.5" />
  }
/>

## With Restricted Dragging Area

<Go
  img=""
  example="Sortable"
  defaultEntryName="SortableWithScrollView"
  defaultFile="WithScrollView/index.tsx"
  entry="WithScrollView"
  rightFooter={
    <GoRightFooter demoName="sortable-with-draggable-area" version="0.0.5" />
  }
/>

## Disable all dragging and sorting behavior

<Go
  img=""
  example="Sortable"
  defaultEntryName="SortableDisableSorting"
  defaultFile="DisableSorting/index.tsx"
  entry="DisableSorting"
  rightFooter={
    <GoRightFooter demoName="sortable-disable-sorting" version="0.0.5" />
  }
/>

## Structure

The whole SortableItem is draggable.

```jsx
 <SortableRoot data={data}>
    {(item: SortableData<string>) => {
      return (
        <SortableItem />
      )
    }}
 </SortableRoot>
```

Only the SortableItemArea is draggable.

```jsx
 <SortableRoot data={data}>
    {(item: SortableData<string>) => {
      return (
        <SortableItem as='DraggableRoot' >
          <SortableItemArea />
        </SortableItem>
      )
    }}
 </SortableRoot>
```

## API
