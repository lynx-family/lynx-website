import { UIApiTable } from '@lynx/index';

## BounceableBasicProps

### Properties

<UIApiTable
  source={[
    {
      name: 'alwaysBouncing',
      type: 'boolean',
      summary: [
        {
          kind: 'text',
          text: 'Whether the scrollable container can bounce when the content area of the scrollable container is smaller than the viewport area.',
        },
      ],
      summary_zh: '当可滚动容器的内容区域小于视口区域时，是否允许容器回弹。',
      defaultValue: 'true',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'debugLog',
      type: 'boolean',
      summary: [
        {
          kind: 'text',
          text: 'Display debug logs. Open it when you find a bug.',
        },
      ],
      summary_zh: '显示调试日志。当您发现错误时，请打开此选项。',
      defaultValue: 'false',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'enableBounceEventInFling',
      type: 'boolean',
      summary: [
        {
          kind: 'text',
          text: 'Trigger bounces effect during fling',
        },
      ],
      summary_zh: '在快速滑动（fling）时触发回弹效果',
      defaultValue: 'false',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'enableBounces',
      type: 'boolean',
      summary: [
        {
          kind: 'text',
          text: 'Whether to enable bounce effect',
        },
      ],
      summary_zh: '是否开启回弹效果',
      defaultValue: '',
      isOption: false,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'endBounceTriggerDistance',
      type: 'number',
      summary: [
        {
          kind: 'text',
          text: 'The threshold distance of lower bounces, in pixels, when triggering the scrollToBounces event',
        },
      ],
      summary_zh:
        '触发 `scrollToBounces` 事件时，下部回弹的阈值距离（单位：px）',
      defaultValue: '0',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'estimatedHeight',
      type: 'number',
      summary: [
        {
          kind: 'text',
          text: 'Unit: px. Optional size hint for bounce effect. Recommended when used in List or <list/>.',
        },
      ],
      summary_zh:
        '单位：px。可选的回弹效果尺寸提示。在列表或 `<list/>` 中使用时推荐设置。',
      defaultValue: '',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'estimatedWidth',
      type: 'number',
      summary: [
        {
          kind: 'text',
          text: 'Unit: px. Optional size hint for bounce effect. Recommended when used in List or <list/>.',
        },
      ],
      summary_zh:
        '单位：px。可选的回弹效果尺寸提示。在列表或 `<list/>` 中使用时推荐设置。',
      defaultValue: '',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'lowerBounceItem',
      type: 'ReactElement',
      summary: [
        {
          kind: 'text',
          text: 'Content of lower bounces view, which will be displayed during the lower bouncing effect.',
        },
      ],
      summary_zh: '下部回弹视图的内容，将在下部回弹效果期间显示。',
      defaultValue: '',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'singleSidedBounce',
      type: 'upper | lower | both | iOSBounces | none',
      summary: [
        {
          kind: 'text',
          text: "The direction of the bounce effect. 'both' for bouncing on both ends, 'none' to disable bounce, and 'upper'/'lower' for bouncing on either end",
        },
      ],
      summary_zh:
        "回弹效果的方向。'both' 表示两端都回弹，'none' 表示禁用回弹，'upper'/'lower' 表示只在上端或下端回弹。",
      defaultValue: "'both'",
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'startBounceTriggerDistance',
      type: 'number',
      summary: [
        {
          kind: 'text',
          text: 'The threshold distance of upper bounces, in pixels, when triggering the scrollToBounces event',
        },
      ],
      summary_zh:
        '触发 `scrollToBounces` 事件时，上部回弹的阈值距离（单位：px）',
      defaultValue: '0',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'upperBounceItem',
      type: 'ReactElement',
      summary: [
        {
          kind: 'text',
          text: 'Content of upper bounces view, which will be displayed during the upper bouncing effect.',
        },
      ],
      summary_zh: '上部回弹视图的内容，将在上部回弹效果期间显示。',
      defaultValue: '',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
    {
      name: 'validAnimationVersion',
      type: 'boolean',
      summary: [
        {
          kind: 'text',
          text: "requestAnimationFrame don't work under lynx version 2.15.2. This switch should be opened when the version is higher than 2.15.2.",
        },
      ],
      summary_zh:
        '`requestAnimationFrame` 在 lynx 2.15.2 以下版本中不起作用。当版本高于 2.15.2 时，应打开此开关。',
      defaultValue: 'false',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
  ]}
/>
### Events
<UIApiTable
  source={[
    {
      name: 'onScrollToBounces',
      type: '(e: scrollToBouncesInfo) => void',
      summary: [
        {
          kind: 'text',
          text: 'When bounces effect is triggered and the bounce distance is larger than startBounceDistance or endBounceDistance on the upper or lower side, the scrollToBounces event will be triggered.',
        },
      ],
      summary_zh:
        '当触发回弹效果并且回弹距离在上侧或下侧大于 `startBounceDistance` 或 `endBounceDistance` 时，将触发 `scrollToBounces` 事件。',
      defaultValue: '0',
      isOption: true,
      isSupportIOS: true,
      isSupportAndroid: true,
      isSupportHarmony: true,
      docTypeFallback: null,
    },
  ]}
/>

### scrollToBouncesInfo

<UIApiTable
  source={[
    {
      name: 'direction',
      type: 'upper | lower',
      summary: null,
      summary_zh: '',
      defaultValue: '',
      isOption: false,
      isSupportIOS: false,
      isSupportAndroid: false,
      isSupportHarmony: false,
      docTypeFallback: null,
    },
  ]}
/>
